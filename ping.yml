name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -A "GitHub Actions KeepAlive" -fsS --retry 3 --max-time 30 https://arb-scanner-free.onrender.com || exit 1
